# このプロジェクトについて(Claude Codeへ)

これはブラウザだけで動く「大海原を操れ！魔法の操舵輪」という**ゲーム**です。
カメラで手の傾きを検出して舵角に変換し、正面から迫る岩をよけます。
**両手操作・片手操作の両対応**で、難易度は3段階(easy/normal/hard)。
ビルドやインストールは不要で、以下のファイルだけで完結します。

| ファイル | 役割 |
| --- | --- |
| `index.html` | 画面の構造(操舵輪のSVG、スコア表示、スタート/ゲームオーバー画面) |
| `style.css` | 見た目。色は `:root` にまとめてある |
| `main.js` | カメラと手の検出 → 舵角の算出。60fpsの更新ループもここが持つ |
| `game.js` | 舵角を受け取って船を動かし、海と岩を `<canvas>` に描くゲーム本体 |
| `sound.js` | 音。**音声ファイルは持たず、Web Audio API で全部その場で合成している** |

手の検出は MediaPipe Hands をCDN(jsdelivr)から読み込んで使っています。
公開先は GitHub Pages: https://pa314960-prog.github.io/kaken.kazi.cam/

## ユーザーから「セットアップして」「動かして」と言われたら

1. このフォルダに `index.html` があるか確認する。
2. ローカルで動作確認する場合は、カメラ(getUserMedia)はセキュアコンテキストが必要なので
   `file://` では正しく動かない。必ず簡易HTTPサーバー経由で開くこと。
   - Pythonがあれば: `python -m http.server 8000` を実行し、
     `http://localhost:8000` をブラウザで開くよう案内する。
   - Pythonが無ければ Node.js の `npx http-server -p 8000` を提案する。
3. ブラウザは Google Chrome か Microsoft Edge を推奨(Chromium系)。
4. サーバーを起動したら、ブラウザでページを開くところまではユーザー自身の操作(UI操作)に
   なるため、「ブラウザで http://localhost:8000 を開いてください」と明確に案内すること。
5. カメラの使用許可はブラウザのポップアップでユーザーがクリックする必要がある。これも
   コードでは自動化できないUI操作なので、必ずその旨を伝える。

## 動作確認するときの注意

- **キーボードの ← → キーでも舵を操作できる**ので、カメラ無しでも動作確認できる。
- ブラウザの**バックグラウンドタブでは `requestAnimationFrame` が絞られる**ため、
  ゲームがほとんど進まないように見える。これは不具合ではない。自動で検証したい場合は
  `RudderGame.tick(1/60, 舵の値)` を直接ループで呼ぶと、rAFに依存せず進められる。
- ローカルサーバーで確認するとき、`python -m http.server` はキャッシュを効かせるため、
  CSSやJSを書き換えてもブラウザが古いものを読むことがある。**スーパーリロード**するか、
  `fetch(url, {cache:'reload'})` してから再読み込みすること。

## コードを変更するときの方針

- 依存関係はCDN読み込みのみ(npm installは使わない)。パッケージ管理を増やすと
  「他のPCでもすぐ動く」という要件が崩れるため、npm化などの大きな構成変更は
  ユーザーに確認してから行うこと。
- **むずかしさや操作感の調整は、まず `game.js` 冒頭の `DIFFICULTY_PRESETS`(難易度ごと)と
  `GAME_CONFIG`(共通)の数値で対応できないか検討する。** ロジックを増やす前にここを見ること。
  各項目の意味は README.md の表にある。
- 難易度を増やすときは `DIFFICULTY_PRESETS` に項目を足し、`index.html` の難易度ボタンに
  `data-difficulty="キー名"` のボタンを追加する。main.js 側の変更は不要。
- 手の検出まわりの調整は `main.js` 冒頭の `CONFIG`。
- UIの文言は日本語。ユーザーはプログラマーではない可能性があるので、
  説明的な変数名・コメントを保つこと。

## 手の検出まわりの仕様(壊さないよう注意)

- 検出は `maxNumHands: 2`。**手が2つ見えたら両手モード、1つなら片手モード**に自動で切り替わる。
  - 両手 … 左右の中指の付け根(landmark 9)を結ぶ線の傾き。左右どちらの手が先に検出されても
    同じ結果になるよう、ベクトルを必ず左→右の向きにそろえてから角度を出している。
  - 片手 … 手首(0)から中指の付け根(9)への向き。
- **キャリブレーションの基準値は片手用・両手用で別々に持っている**(`calibrationOffset.one` /
  `.two`)。自然な構えが違うため、1つで共用すると片手↔両手が切り替わった瞬間に舵が跳ねる。
  ここを1つに戻さないこと。
- 操舵輪には「輪を握る手」の絵(`#grip-left` / `#grip-right`)が乗っていて、輪と一緒に回る。
  そのため **`CONFIG.wheelTurnRatio` は 1.0(等倍)である必要がある。**
  ここを大きくすると輪は派手に回るが、描かれた手が利用者の実際の手の傾きとずれて不自然になる。

## 音まわりの仕様

- **mp3などの音声ファイルは置かない方針。** `sound.js` が Web Audio API で波・風・汽笛・
  衝突音などをすべて合成している。音を足すときも、ファイルを増やさずここに関数を追加すること。
- ブラウザの自動再生制限があるため、**`AudioContext` は利用者のクリックまで作らない**。
  `main.js` の document クリックリスナーが `Sound.init()` を呼ぶ(何度呼んでも安全)。
  クリック前に `Sound.init()` を呼ぶと `suspended` のままになり、音が出ずに悩むことになる。
- 出口に `DynamicsCompressor` のリミッターを入れてある。音が重なっても音割れしない
  (全効果音＋全速の環境音を同時に鳴らしてピーク0.81で確認済み)。
- 音量調整は `sound.js` 冒頭の `VOLUME`。

## 描画まわりの落とし穴(はまりやすい点)

- SVGの `viewBox` を `"-200 -200 400 400"` のように**負の原点**で取っているため、
  CSSの `transform-origin` は **`0 0` が図形の中心**になる。ここを `50% 50%` にすると
  図形が大きくずれるので注意(実際に一度これで渦が画面外に飛んだ)。
- `display: flex` などを指定した要素は、HTMLの `hidden` 属性だけでは消えない。
  `.foo[hidden] { display: none; }` を併記すること。
- ゲーム画面(`<canvas>`)は `game.js` が描いている。CSSで背景を作っているわけではない。

## デプロイ

GitHub Pages で公開済み(ブランチ `claude/camera-hand-gesture-rudder-4gll2r` の `/(root)`)。
`main` ブランチではないので注意。push すれば数十秒〜数分で反映される。
公開設定の変更は GitHub の Settings > Pages 画面でのUI操作が必要で、コードからは自動化できない。
