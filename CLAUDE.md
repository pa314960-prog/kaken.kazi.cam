# このプロジェクトについて(Claude Codeへ)

これはブラウザだけで動く「カメラの手ジェスチャーで舵(rudder)を動かす」静的Webページです。
ビルドやインストールは不要で、`index.html` / `style.css` / `main.js` の3ファイルと
`CNAME`(GitHub Pages用のカスタムドメイン設定)だけで完結します。手の検出は
MediaPipe Hands をCDN(jsdelivr)から読み込んで使っています。

## ユーザーから「セットアップして」「動かして」と言われたら

1. このフォルダに `index.html` があるか確認する。
2. ローカルで動作確認する場合は、カメラ(getUserMedia)はセキュアコンテキストが必要なので
   `file://` では正しく動かないことがある。必ず簡易HTTPサーバー経由で開くこと。
   - Pythonがあれば: `python -m http.server 8000` を実行し、
     `http://localhost:8000` をブラウザで開くよう案内する。
   - Pythonが無ければ Node.js の `npx http-server -p 8000` を提案する。
   - どちらも無ければ、GitHub Pages 経由での公開(README.md参照)を提案する。
3. ブラウザは Google Chrome か Microsoft Edge を推奨(Chromium系)。
4. サーバーを起動したら、Windowsのブラウザでページを開くところまではユーザー自身の操作(UI操作)
   になるため、「ブラウザで http://localhost:8000 を開いてください」と明確に案内すること。
5. カメラの使用許可はブラウザのポップアップでユーザーがクリックする必要がある。これも
   コードでは自動化できないUI操作なので、必ずその旨を伝える。

## コードを変更するときの方針

- 依存関係はCDN読み込みのみ(npm installは使わない)。パッケージ管理を増やすと
  「他のPCでもすぐ動く」という要件が崩れるため、npm化などの大きな構成変更は
  ユーザーに確認してから行うこと。
- 挙動の調整(感度・最大舵角・スムージング)は `main.js` 冒頭の `CONFIG` や
  `sensitivity` / `maxAngle` の初期値を変えるだけで済むようにしてある。
  ロジックを増やすより、まずこの値の調整で対応できないか検討する。
- UIの文言は日本語。ユーザーはプログラマーではない可能性があるので、
  説明的な変数名・コメントを保つこと。

## デプロイ

`CNAME` ファイルにより GitHub Pages のカスタムドメイン `kaken.kazi.cam` を想定している。
ただし GitHub Pages の有効化(リポジトリの Settings > Pages)と、ドメインの DNS 設定は
どちらもWeb UI上の操作でありコードから自動化できないため、必ずユーザーに手順として案内すること。
